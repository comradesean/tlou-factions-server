# --- Copyrighted game data / large binaries (never commit) ---
*.elf
EBOOT.*
# S3 content mirror (game assets, player profiles)
server/data/served_content/
research/net1bin/
ref/

# --- Vendored/cloned external tooling (referenced by URL+commit in docs) ---
research/tools/external/

# --- Virtualenvs / caches ---
**/.venv/
__pycache__/

# --- Server runtime output (live, not source) ---
server/logs/
# runtime leaderboard store
server/data/leaderboard_scores.sqlite

# --- Packet captures & live logs (unfiltered; keep local) ---
captures/*.pcap
captures/*.pcapng
captures/*.log
*-run.log
*_debug.log

# --- Ghidra project + raw RE dumps (keep distilled findings, not raw output) ---
research/ghidra/*.gpr
research/ghidra/*.rep/
research/ghidra/*.lock
research/ghidra/*.lock~
research/ghidra/*.log
research/ghidra/*.txt
research/disasm/

# --- Generated Kaitai parsers (build artifacts of protos/*.ksy) ---
protos/*.py

# --- OS / editor cruft ---
.DS_Store
*.swp

# --- Stray Ghidra export blobs written to the repo root ---
/003433d0
/012bc660
/0x00ad33d8
